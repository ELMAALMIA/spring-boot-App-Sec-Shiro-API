package com.dev.app.enums;

/**
 * Allowed role values.
 * Stored as a STRING in the database (e.g. "ADMIN", "USER").
 * Add new roles here when needed.
 */
public enum RoleName {
    ADMIN,
    USER
}
